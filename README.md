## Usage
1. Install dependencies
    ```bash
    npm install
    ```
2.  Run the application
     ```bash
    npm run start    
    ```
   
3. Open browser
    ```
        http://localhost:3000/
    ```
